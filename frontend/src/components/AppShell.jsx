export default function AppShell({ user, page, setPage, onSignOut, children }) {
  const links = [["dashboard", "Dashboard"], ["create", "Create"], ["schedule", "Schedule"], ["explore", "Explore"]];
  return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">M</span><span>Mashbot</span></div><p className="side-label">Workspace</p><nav>{links.map(([id, label]) => <button key={id} className={page === id ? "nav-item active" : "nav-item"} onClick={() => setPage(id)}><span className={`nav-dot ${id}`} />{label}</button>)}</nav><p className="side-label">Account</p><nav><button className={page === "profile" ? "nav-item active" : "nav-item"} onClick={() => setPage("profile")}><span className="nav-dot profile" />Profile</button>{user.role === "admin" && <button className={page === "people" ? "nav-item active" : "nav-item"} onClick={() => setPage("people")}><span className="nav-dot people" />People & roles</button>}</nav><div className="sidebar-footer"><div className="avatar">{user.name?.slice(0, 1).toUpperCase()}</div><div><strong>{user.name}</strong><small>{user.role}</small></div><button className="logout" onClick={onSignOut}>Sign out</button></div></aside><main className="content"><header className="topbar"><div><p className="eyebrow">MASHBOT / CONTROL ROOM</p><h1>{links.find(([id]) => id === page)?.[1] || (page === "people" ? "People & roles" : "Profile")}</h1></div><div className="topbar-meta"><span className="live-dot" />Workspace live</div></header>{children}</main></div>;
}
